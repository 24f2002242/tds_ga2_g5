name: CI with Cache

on:
  push:
    branches: [ main ]
  pull_request:


      
jobs:
  build:
    runs-on: ubuntu-latest
  test:
    steps:
      - name: 24f2002242@ds.study.iitm.ac.in
        run: echo "Hello, world!"
